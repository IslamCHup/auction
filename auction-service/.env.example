## Auction Service environment variables

# PostgreSQL connection string for GORM
# Example for local development:
# host=localhost user=postgres password=postgres dbname=postgres port=5432 sslmode=disable
DATABASE_URL=host=localhost user=postgres password=postgres dbname=postgres port=5432 sslmode=disable

# Kafka brokers for producing domain events (bid_placed, lot_completed)
# In docker-compose the broker is available as kafka:9092
KAFKA_BROKERS=kafka:9092

# Base URL of the User & Wallet service used for wallet freeze/unfreeze
# Example: http://user-wallet:8080 (docker) or http://localhost:8082 (local)
WALLET_SERVICE_URL=http://user-wallet:8080